import 'package:flutter/material.dart';

class Homeview extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text("home page bichies")),
        body: Center(
          child: Text("ini halman home"),
        ));
  }
}
